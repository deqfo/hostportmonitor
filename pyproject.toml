[project]
name = "hostportmonitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kristián Eliaš", email = "kristiandeqfo@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
hostportmonitor = "hostportmonitor:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
