[project]
name = "hostportmonitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kristián Eliaš", email = "kristiandeqfo@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
hostportmonitor = "src.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]
